import React from 'react';

const GraphSection: React.FC = () => {
  return (
    <div className="w-full h-64 bg-cards rounded shadow p-4">
      <h2 className="text-lg font-bold">Graphs depicting the progress of projects</h2>
      {/* Graph component would go here */}
    </div>
  );
};

export default GraphSection;
